[['console.firebase.google.com](https://console.firebase.google.com)\n2. **Node.js**: v18+ installed locally\n3. **Firebase CLI**: `npm install -g firebase-tools`\n4. **Authentication**: `firebase login`\n\n## Configuration Steps\n\n### 1. Set Up Firebase Project\n\n1. Create a new Firebase project (or use existing)\n2. Enable **Firestore Database** in Native mode\n3. Enable **Authentication** (Email/Password, Google, etc.)\n4. Enable **Hosting**\n\n### 2. Configure Environment Variables\n\nCopy `.env.example` to `.env` and fill in your Firebase configuration:\n\n```bash\ncp .env.example .env\n```\n\nGet values from Firebase Console > Project Settings > General > Your Apps > Firebase SDK snippet > Config.\n\n### 3. Update Firestore Rules\n\nDeploy the security rules:\n\n```bash\nfirebase deploy --only firestore:rules\n```\n\nThe rules in `src/lib/firestore.rules` provide multi-tenant access control.\n\n### 4. Build the Application\n\nThe app uses TanStack Start with Vite. Build for production:\n\n```bash\nnpm run build\n```\n\nThis creates the `dist/client` folder for Firebase Hosting.\n\n### 5. Deploy to Firebase Hosting\n\n```bash\nfirebase deploy --only hosting\n```\n\nYour app will be live at `https://your-project.web.app`\n\n## Seeding Sample Data\n\nTo populate Firestore with sample data for testing:\n\n### Option A: Using Firebase Admin SDK (Recommended)\n\n1. Install dependencies:\n   ```bash\n   npm install firebase-admin\n   ```\n\n2. Authenticate with Google Cloud:\n   ```bash\n   gcloud auth application-default login\n   ```\n\n3. Run the seed script:\n   ```bash\n   node scripts/seed-firestore.mjs\n   ```\n\n   To clear existing data first:\n   ```bash\n   node scripts/seed-firestore.mjs --clear\n   ```\n\n### Option B: Using Firebase Console\n\n1. Go to Firestore > Data\n2. Create collections manually:\n   - `tenants`\n   - `warehouses`\n   - `locations`\n   - `itemMaster`\n   - `inventoryItems`\n   - `inboundShipments`\n   - `orders`\n   - `ediLogs`\n3. Add sample documents from `src/lib/mock-data.ts`\n\n## Real-Time Testing\n\nThe application uses Firestore real-time listeners for live data updates:\n\n- **Tenants & Warehouses**: Auto-subscribe on dashboard load\n- **Inventory**: Real-time stock level updates\n- **Orders**: Live order status changes\n- **EDI Logs**: Real-time EDI transaction monitoring\n\nTo test real-time features:\n\n1. Open the app in two browser windows\n2. Make changes in one window (e.g., update inventory quantity)\n3. Observe automatic updates in the other window\n\n## Transactional Read/Write Operations\n\nFirestore supports transactions for atomic operations. Example use cases:\n\n### 1. Inventory Deduction (Allocate Stock)\n\n```typescript\nimport { runTransaction } from "firebase/firestore";\nimport { db } from "@/lib/firestore', 'async function allocateInventory(orderId: string, items: Array<{sku: string, qty: number}>) {\n  return runTransaction(db, async (transaction) => {\n    for (const {sku, qty} of items) {\n      const invRef = doc(db, "inventoryItems', {'qty': 'currentQty - qty'}], {'orders': 'orderId);\n    const orderSnap = await transaction.get(orderRef);\n    \n    transaction.update(orderRef', 'status': 'newStatus', 'updatedAt': 'new Date().toISOString()'}, {'oldStatus': 'orderSnap.data()?.status', 'timestamp': 'new Date().toISOString()'}]